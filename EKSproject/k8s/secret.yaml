# =========================================================
# 🔐 vProfile Kubernetes Secrets (Sensitive Configuration)
# =========================================================
# Purpose:
# - Stores sensitive credentials securely in Kubernetes
# - Used by MySQL and RabbitMQ deployments
# - Values are base64 encoded (not plain text)
# =========================================================
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  db-pass: dnByb2RicGFzcw==   # MySQL root password (base64)
  rmq-pass: Z3Vlc3Q=          # RabbitMQ password (base64)
